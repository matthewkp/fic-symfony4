{% extends 'base.html.twig' %}

{% block body %}
<div class="col-sm-8 offset-sm-2">
    <p>{{ 'welcome.message'|trans }}</p>
    <p>{{ 'articles.count.text'|transchoice(totalArticles) }}</p>
    <p>{{ languages }}</p>
</div>
{% endblock %}

{% block sidebar %}{% endblock %}
